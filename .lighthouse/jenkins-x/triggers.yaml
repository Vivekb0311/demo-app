apiVersion: config.lighthouse.jenkins-x.io/v1alpha1
kind: TriggerConfig
spec:
  presubmits:
  - name: pr
    context: "pr"
    always_run: true
    optional: false
    source: "pullrequest.yaml"
  postsubmits:
  # Run release on push to main/master or version tag (e.g. v0.1.2)
  - name: release
    context: "release"
    source: "release.yaml"
    branches:
    - ^main$
    - ^master$
    - ^v[0-9]+\.[0-9]+\.[0-9]+$
